apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-test-config
  namespace: media-test
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-retain-rwo
  resources:
    requests:
      storage: 2Gi
