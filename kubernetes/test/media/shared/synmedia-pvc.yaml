apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: synmedia-nfs
  namespace: media-test
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: synmedia-nfs-test
  resources:
    requests:
      storage: 10Ti
