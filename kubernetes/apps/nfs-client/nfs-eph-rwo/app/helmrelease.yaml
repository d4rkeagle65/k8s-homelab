# Generated by k8s-backup-repo-gen on 2026-09-20T23:27:46Z.
# Do not hand-edit; this file is overwritten on the next run.
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nfs-eph-rwo
  namespace: nfs-client
spec:
  interval: 30m
  chart:
    spec:
      chart: nfs-subdir-external-provisioner
      version: 4.0.18
      sourceRef:
        kind: HelmRepository
        name: nfs-subdir
        namespace: flux-system
  values:
    nfs:
      path: /volume1/k8sData
      server: ${NFS_SERVER}
    storageClass:
      defaultClass: true
      name: nfs-eph-rwo
      provisionerName: ${CLUSTER_DOMAIN}/nfs-eph-rwo
